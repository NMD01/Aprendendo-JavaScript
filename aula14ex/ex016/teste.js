var n = 14
var max = 30
var pular = 3


for(n ; n <= max ; n += pular ){
    console.log(`${n} passo`)
    
}

var n = Number(n1.value)
        var max = Number(max1.value)
        var pular = 1
        var emoji = String.fromCodePoint (0x27A1)

    for(n ; n <= max ; n += pular){
        res.innerHTML += ` ${emoji} ${n}`}
/*if(n.value.length == 0){
    window.alert('[ERRO] Impossivel calcular sem o numero inicial')
}if(max.value.length == 0 || (Number(max.value) == 0)){
    window.alert('[Vc não especificou até q numero sera realizado o calculo, então sera feita a conta até 20]')
    var max = 20

}if(pular.value.length == 0 || (Number(pular.value) == 0)){
    window.alert('[Vc não especificou o numero para pular, então sera pulado em 2 em 2 ]')
    var pular = 2*/



    //0x27A1
    //0xFE0F

    (n1 >= max1 || pular1 > n1 || max1 < pular1 ){
        window.alert('[ERRO]Verifique os dados e tente novamente')}



        else if(pular1.value > max1.value || pular1.value > n1.value){
            window.alert('[ERRO]')
            res.innerHTML = ''


            if(n1.value > 0 && max1.value > 0 && pular1.value > 0){
     
 
                var n = Number(n1.value)
                var max = Number(max1.value)
                var pular = Number(pular1.value)
                var emoji = String.fromCodePoint (0x27A1)
           
            for(n ; n <= max ; n += pular){
                res.innerHTML += ` ${emoji} ${n}`}